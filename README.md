# ERC-UCU
Mars Rover project by UCU Robotics Team for ERC competitions


## Docker

#### Build the Docker image
Run this command in a root directory
```bash
docker build -t ros2-humble -f docker/Dockerfile .
```

#### Run a container
```bash
docker run -it ros2-humble
```

